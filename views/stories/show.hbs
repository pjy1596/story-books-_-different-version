<div class="row">
    <div class="col s12 m8">
        <h3>{{story.title}} <small>{{{editIcon story.user user story._id false}}}</small> </h3>
        <div class="card story">

            <div class="card-content">
                <span class='card-title'>{{formatDate story.createdAt 'MMMM Do YYYY, h:mm:ss a'}}</span>
                <p>{{stripTags story.body}}</p>
            </div>
        </div>
    </div>
    <div class="col s12 m4">
        <div class="card center-align">
            <div class="card-content">
                <span class='card-title'>{{story.user.fullName}}</span>

                <img src="{{story.user.image}}" alt="" class="circle responsive-img img-small">

            </div>
            <div class=" card-action">
                <a href="/stories/user/{{story.user._id}}">MORE FROM {{story.user.fullName}}</a>
            </div>
        </div>
    </div>
</div>
{{!-- 그냥 user가 아니고 story.user이다, 전역변수 user 까먹음, 그림은 src=로 넣어줘야 됨, materialize 문서 참고 --}}
{{!-- card story와 card-story는 다르다, div 넣고 뺐더니 전혀 달라지는 결과들 --}}
{{!-- story.user가 어차피 id 형태이긴 한데, 그냥 story의 user라고 일컬음. 물론 story.user._id도 가능 --}}